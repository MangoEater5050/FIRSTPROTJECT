temp = float(input("Temperature? "))
unit = input("Convert to F or C? ")

if unit.upper() == "C":
    convert = (temp * 9/5) + 32
    print(f"Temperature in fahrenheit is {convert}")
elif unit.upper() == "F":
    convert = (temp - 32) * 5/9
    print(f"Temperature in degrees is {convert}")

